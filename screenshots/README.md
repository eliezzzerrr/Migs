# Screenshots

Drop chart screenshots here (or paste/upload into the conversation directly).

Recommended (per setup type):

**Setup #7 (Internal Breaker Retest scalp):**
- 5m chart (mandatory) — execution TF, all structure lives here
- 15m optional — helps confirm the broader leg if the breaker is a 15m level

**Other setups (HTF baseline #01/#02, future #1–6, #8+):**
- 5m chart (mandatory)
- 15m chart (recommended)
- 1H chart (mandatory for HTF-bias setups)
- 4H chart (only if the setup explicitly leans on HTF bias)

Annotations on: OB (grey/dark rectangle), FVG (yellow rectangle), money-bag DOL markers, sweep wicks circled, breaker level marked.

Filename convention (when saved here): `YYYY-MM-DD-HHMM-[5m|15m|1h|4h].png`

The agent references the screenshot path in the journal entry's `screenshot_path` field. Don't delete files referenced by journal entries — they're the audit trail.
